<view id='ListBackground'>
  <view class="TicketBackground"wx:for="{{order}}" catchtap='toOrderDetail' data-orderInfo='{{item}}' wx:if='{{item.line_info[0]}}'>
    <view class="distance">
      <view>{{item.line_info[0].start}}</view>
      <i class='iconfont icon-icon_jipiaodancheng'></i>
      <view>{{item.line_info[0].end}}</view>
    </view>
    <view class='DepartureTime'><view>订票人:</view><view>{{item.name}}</view></view>
    <view class="place"><view>发车时间:</view><view>{{item.car_info[0].departureTime}}</view></view>
    <view class='place'><view>发车地点:</view><view wx:if='{{item.line_info[0].departurePlace}}'>{{item.line_info[0].departurePlace}}</view><view wx:else>随后通知</view></view>
    <view class='provider'><view catchtap='toContacts' data-phone='{{item.car_info[0].contacts_phone}}'><view>联系人:</view><view class="call-btn">{{item.car_info[0].contacts_name}}<span><i class="iconfont icon-shoujihao"></i>联系我们</span></view></view>
    <view class='Toticket' catchtap='GoPopup' wx:if = '{{!item.refound && !item.car_info[0].complete}}' data-info='{{item}}' data-index = '{{index}}'>退票</view>
    <view class='Torefound' wx:if = '{{item.refound}}'>已退票</view>
    <view class='Traveled' wx:if = '{{item.car_info[0].complete && !item.refound}}'>已出行</view>
    </view>
    <view class='provider' wx:if='{{item.car_info[0].remark}}'>
      <view><view>备注信息:</view><view>{{item.car_info[0].remark}}</view></view>
    </view>
    <!-- <view class='provider'>
      <view><view>备注信息:</view><view>{{item.car_info[0].remark}}</view></view>
    </view> -->
  </view>
</view>
<view class='default-box' wx:if="{{order.length <= 0}}">
   <view class='default-center'>
     <view class='icon'><i class='iconfont bashi'></i></view>
     <view>您还没有乘车订单</view>
     <view>
       <p>1.选择车次</p>
     </view>
     <view>
       <p>2.支付票款</p>
     </view>
     <view>
      <p>3.乘车出行</p>
     </view>
     <button bindtap='toTicket'>去购票</button>
   </view>
</view>